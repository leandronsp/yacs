version: '3.9'

services:
  dev:
    image: ruby:2.7
    container_name: yacs
    working_dir: /yacs
    volumes:
      - ./:/yacs
      - rubygems:/usr/local/bundle
    ports:
      - "80:80"
    networks:
      yacs:
        aliases:
          - yacs.dev

volumes:
  rubygems:

networks:
  yacs:
